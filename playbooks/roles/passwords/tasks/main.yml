---
- name: Create /etc/opt/lucid-hpc/passwords/
  become: true
  file:
    path: /etc/opt/lucid-hpc/passwords/
    state: directory
    owner: "{{ ansible_user }}"
    mode: 0770
    group: "{{ ansible_user }}"
    recurse: yes
